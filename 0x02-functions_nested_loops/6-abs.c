#include "main.h"

/**
 * _abs - computes absolute valuse of an integer
 * @x: The character to compute
 *
 * Return: Nothing
 */

int _abs(int x)
{
	int c = -1;
	int i = x * c;

	if (x < 0)
	{
		_putchar(i);
	}
	else if (x >= 0)
	{
		_putchar(i);
	}
	return (0);
}
