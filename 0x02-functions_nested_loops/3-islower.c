#include "main.h"

/**
 * _islower - determines if the charcter c is lower case
 * @c: The character to check
 *
 * Return: On success 1
 * On error: 0 is returned
 */

int _islower(int c)
{
	if (c >= 'a' && c <= 'z')
	{
		return (1);
	}
}
